# Twinkle 架构定稿

> 生成日期：2026-08-05
> 状态：最终定稿，作为新项目（twinkle）的实施依据
> 目标：做一个**热更新、扩展性好**的冒险岛后台。参考项目：北斗（`E:\LocalGit\GitHub\BeiDou-Server`）

---

## 一、设计原则

### 1.1 三条铁律

1. **一套代码、多种拓扑**：单进程 ↔ 单机多进程 ↔ 跨机分布式，**配置驱动，一条命令切换**。单进程含两档：`single`（全内嵌、性能极致）与 `standalone`（SQLite 低配），见 4.1。任何涉及"另一个东西"的接口，从第一天起不假设它在进程内。判据：`单进程下 X 是什么形态？分布式下 X 变成什么？`
2. **性能是硬指标**：现状 16C32G 单进程抗 2000+ 人、20 频道，重构后不能退步；低配 2C2G 单进程也必须能跑。任何默认引入重服务、常驻大内存、独立进程的方案都违反此线。
3. **状态与逻辑分离**：游戏实体 = 纯数据（版本化 schema）+ 操作数据的逻辑系统。这是热重载、分布式、插件三个诉求的共同地基。

### 1.2 五大目标（用户原始诉求）

| 目标 | 方案 |
|---|---|
| **1. 原生 HTTP** | Micronaut MVC；`/internal/v1/*`（官网转调）+ `/api/v1/*`（第三方，限流+版本化）。HTTP 线程投递游戏线程，禁止直踩游戏内存对象 |
| **2. 保留 Netty** | v83 协议 / 加解密 / RecvOpcode 字节级不动；`HandlerRegistry` 可 register/replace |
| **3. 热更新** | 进程内真无感热重载（L1-L3）+ L4 进程兜底（见第五节） |
| **4. 多数据源** | 非第一约束。单库为主；将来若确实需要，接口先行、按需引入（同铁律 1） |
| **5. 插件** | VSCode 式插件系统，与热重载同一套 classloader / 贡献点机制（见第七节） |

### 1.3 核心模型

**内存态是权威，DB 只是持久化 + 查询层。** 游戏热路径（tick/战斗/交易）全在内存，不碰数据库。

---

## 二、技术选型

### 2.1 选型总表

| 层 | 选型 | 决策理由 |
|---|---|---|
| 框架 / DI / HTTP | **Micronaut 4** | 轻量、编译时 DI、启动快内存小 |
| JDK | **GraalVM Community Edition for JDK 21**（基于 OpenJDK，内置 JVMCI + GraalVM JS） | 原生带 JVMCI → GraalJS 全速无需额外开关；启动快、内存可控；社区中立发行版 |
| 持久化 / ORM | **MyBatis-Flex** | 无 JPA 生命周期负担；无 N+1；查出来即普通数据，贴合"数据在内存"模型 |
| 数据库 | **SQLite**（低配 standalone 默认）/ **PostgreSQL**（大服/分布式）/ MySQL 8+、MariaDB（兼容切换） | 按规模三档，接口化按需切换；方言全覆盖 |
| AI | **LangChain4j** | 流式 + 工具调用原生合一 |
| 客户端网络 | **Netty 4** | v83 协议 / 加解密 / RecvOpcode 全部不动 |
| 脚本 | **GraalVM JS**（独立库引入） | 保留，宿主对象契约（cm/qm/em/rm/im）不变 |
| 数据迁移 | **自研迁移器**（M0 决策，替换 Flyway） | 每数据源独立版本表；Flyway 社区版自 V10 撤 SQLite（商业版才有），架构默认 SQLite，故三库统一自研（见 6.2） |
| 日志 | log4j2 | 保留 |
| 限流 | Bucket4j | 第三方 API |
| 国际化 | **服务端固定语言 + Java ResourceBundle；Web 独立类型化语言目录** | `twinkle.service.language` 统一控制全部 Java 后台文案；WZ/脚本仍由显式目录决定；机器契约不翻译 |
| UI | **Web 控制台**：前端 **React 19 + Vite + TypeScript**，组件层采用 **shadcn** `radix-nova` preset（Radix + Tailwind CSS v4 + Lucide）；以 shadcn 官方组件风格和语义令牌为视觉权威；独立工程 `twinkle-web/`，浏览器端不受 2C2G 红线约束 | JavaFX 明确移除 |
| 缓存 / 对象 / MQ | **不默认引入** | 内存态权威，进程内实现即可；standalone 不常驻重服务 |

### 2.2 关键取舍记录

- **ORM 用 MyBatis-Flex**：无 JPA 生命周期负担、无 N+1、查出来即普通数据，贴合"内存态权威"模型。DB 层非热路径，ORM 开销落空；真成热点的单条查询降级原生 SQL，业务代码不动。
- **缓存 / 对象 / MQ 不引入**：2C2G 红线 + 内存态权威。共享状态进程内实现，接口打底，分布式按需替换。
- **JDK 锁定 GraalVM CE for JDK 21**：原生内置 JVMCI 与 GraalVM JS，**选定即自带「全速」前提**，不再依赖 `-XX:+EnableJVMCI` 或外部挂载 `org.graalvm.compiler`；GraalVM JS 以独立 Maven 库（`org.graalvm.js:js`）形式引入，仅作版本管理。内存调优走 HotSpot 侧（GraalVM CE 基于 HotSpot）：AppCDS + SerialGC + 收紧堆。
- **SQLite 规模验证（搜索核证）**：WAL 下实测 70K reads/s + 3.6K writes/s，读吞吐 2 倍于 MySQL、p99 读延迟低 19 倍；极限是"持续 >10 并发写/秒"而非"人数"——本架构低频批量写天然避开。**三件套做对即稳**：WAL + 单写连接 + busy_timeout。
- **AI 选定 LangChain4j**：流式 + 工具调用同时支持是硬需求。
- **框架选定 Micronaut 4**：轻量 + 中间件全主流。Vert.x 因响应式风格与命令式游戏逻辑割裂被否。
- **Java 后台语言由服务端配置统一决定**：`twinkle.service.language` 使用 BCP 47（首批 `zh-CN`、`en-US`），日志、异常、HTTP 消息和系统文案统一经 `I18nService` 解析；客户端请求与账号不得覆盖。Web 控制台有独立 UI locale。错误码、opcode、字段 ID、JSON key 等机器契约保持稳定。WZ/脚本不参与该机制，继续按显式路径加载。完整规范见 `docs/i18n.md`。
- **前端框架选定 React 19 + Vite + TypeScript + shadcn**：运维后台统一使用 shadcn Registry 组件，固定 `radix-nova` preset、Radix、Lucide、CSS Variables 与 Tailwind CSS v4。视觉以 shadcn 官方组件、variant、密度和 neutral 浅深主题为准，不再模仿其他品牌设计系统；页面只在业务组合与布局层扩展，避免重复实现基础组件。前端为独立工程 `twinkle-web/`，与 Java 服务端通过 `http-api` 模块（`/api/v1`）解耦，浏览器端不受 2C2G 红线约束。详细纪律见 `twinkle-web/docs/design-system.md`。

---

## 三、工程结构（Maven 多模块）

```
twinkle/
├── 公共底座（所有进程都要）
│   ├── bootstrap/     # 唯一 main：读 --profile 装配角色模块
│   ├── core/          # DI、EventBus、配置门面、调度、插件框架、热更新、RestartCoordinator
│   ├── net-netty/     # Netty IO（客户端 v83 协议 + 内部通信复用，不依赖业务）
│   ├── net-packet/    # v83 opcode、HandlerRegistry、PacketCodec
│   ├── data/          # MyBatis-Flex 映射 + Repository + 自研迁移器（M0 决策）
│   ├── db-dialect/    # 方言差异点封装（upsert/自增/布尔/时间函数等）
│   └── plugin-api/    # 插件 SPI（贡献点、SDK 版本化）
│
├── 游戏域（频道进程）
│   ├── domain-game/   # 游戏对象模型（Character/MapleMap/...，纯数据 + 索引/服务）
│   ├── domain-script/ # GraalVM 脚本引擎
│   ├── wz-provider/   # WZ 解析
│   └── channel/       # 频道、玩家在线表、CC 迁移
│
├── 管理侧（管理进程）
│   ├── coordinator/   # 共享状态代理、定位表、消息路由（内建注册中心/配置中心）
│   ├── login/         # 账号校验、选角
│   ├── admin/         # Web 控制台、运维 API
│   ├── http-api/      # Micronaut Controller（/internal + /api）
│   └── ai/            # LangChain4j Agent + Tool
│
└── plugins/           # 内置插件（按部署作用域分发进对应进程）
```

**模块即进程装配单元**：一个 JVM 进程 = 装配一组模块，装配表即 4.6.2 拓扑的工程化：

| 进程 | 装配模块 |
|---|---|
| **管理进程**（1 个） | 公共底座 + coordinator + login + admin + http-api + ai + 平台插件 |
| **频道进程**（每分片 1 个） | 公共底座 + domain-game + domain-script + wz-provider + channel + 频道插件 |

依赖单向无环，且管理侧**不得依赖 domain-game**——分进程时两者不在同一 JVM（物理隔离），single 档同进程也禁止跨依赖，从编译期杜绝 HTTP/AI 直踩游戏内存。

### 状态 / 逻辑分层

```
稳定层（stable）       协议、数据模型、状态索引、核心机制      ← 不参与重载，被引用
可替换层（replaceable）游戏逻辑系统（systems）、插件          ← 可重载，经接口访问稳定层
```

- 可替换层**经接口访问**稳定层，**绝不反向依赖**。
- 可替换层**不得引用游戏内存对象的具体类**（否则换 classloader 即 CCE），只能引用接口或数据投影。
- 游戏对象（Character/MapleMap/Item）在稳定层，手动 new，不进容器。

---

## 四、运行时拓扑（进程与部署）

### 4.1 可插拔进程边界（最高原则）

**模块可单独打成进程，也可不打成进程——进程边界是配置，不是硬编码。** 同一套代码，按 `--profile` 决定哪些角色捆成一个 JVM：

```
twinkle.jar --profile=single           # 全内嵌：1 个 JVM 跑全部（默认，性能最优档）
twinkle.jar --profile=standalone       # 最低配：单 JVM，SQLite，2C2G/1C1G
twinkle.jar --profile=split-channel    # 频道拆出：coordinator 1 进程 + 每频道 1 进程
twinkle.jar --profile=split-realm      # 按大区拆：coordinator + 每大区 1 进程
```

**关键前提：无论哪种拆分，进程间的"通信接口"与"进程内的"是同一套**（接口不假设进程内外）。配置说"内嵌"→ 走进程内实现（EventBus/内存 Map/函数调用）；配置说"独立"→ 走网络实现（Netty 帧/网络 RPC/跨进程迁移）。**代码不写死，配置文件切换。**

| 接口 | 进程内实现（single） | 独立进程实现（split） |
|---|---|---|
| `bus.send(target, msg)` | 进程内 EventBus（函数调用） | Netty 网络帧 |
| `coordinator.定位(player)` | 进程内 Map | 网络 RPC |
| `channel.load(玩家)` | 进程内方法 | 跨进程状态迁移 |

**默认档 = `--profile=single`（单进程）。** 现状 16C32G 单进程抗 2000+ 人、20 频道，共享数据在一份内存、零拷贝——单进程是性能/吞吐的极致，多进程是买隔离、卖性能。默认档必须保住现状性能，隔离作为按需增强。

**诚实的物理代价**：拆进程不是免费午餐，一旦选 `split-*` 两笔税照付——① 多 JVM 常驻开销（堆+JIT+元空间每进程翻倍）；② 共享数据从"一份内存"变"每进程各一份 + coordinator 同步"。**可插拔解决"不想拆时不拆"，不解决"拆了不降性能"。** "单进程高性能"与"多进程隔离"是配置档，不是二选一的架构。

### 4.2 角色与进程

**角色 ≠ 进程。** 角色是职责的抽象，进程是运行的实例；一个角色可内嵌也可独立成进程。角色：**bootstrap**（启动器，非角色）、**coordinator**（共享状态/定位表/路由）、**channel**（游戏世界）、**login**（账号校验/选角）、**admin**（控制台）。

| 角色 | 职责 | standalone | 分布式 |
|---|---|---|---|
| **bootstrap** | 启动器，读 `--profile` 装配 | 进程起点 | 每个进程的起点 |
| **coordinator** | 共享状态真值 / 玩家定位表 / 调度 / 消息中心路由 | 进程内模块 | 独立 JVM |
| **channel** | 拥有地图/怪物/玩家，一进程托管 N 频道 | 进程内模块 | 独立 JVM |
| **login** | 账号校验 / 选角 | 进程内模块 | 并入管理进程（可选独立） |
| **admin** | Web 控制台 / 运维 API | 进程内模块 | 并入管理进程（可选独立） |

**coordinator 必须无状态**：定位表不靠它自己存，而是各 channel 启动时上报 + 心跳重建；共享状态真值在 DB（coordinator 只是读写代理）。于是 coordinator 崩溃 = 无状态进程重启，频道心跳超时 → 重连 → 重新上报 → 定位表自动重建。**不需要主备选举，高可用被"无状态 + 可重建"消解。**

**注册中心 = coordinator 内建，不独立成组件**：channel 启动向 coordinator 单向上报（频道ID、host:port、在线状态）+ 心跳维护；admin / http / login 查询频道位置也问 coordinator。星形拓扑、coordinator 单点（详见 4.6.4）。

**登录不"走" coordinator**：登录走 login 角色（校验账号/选角）；coordinator 只回答"玩家分配去哪个频道"，玩家进频道后才上报定位表。

**2C2G 强制单进程**：多 JVM 常驻开销每多开一个整体翻倍，2G 预算只够一个 JVM。"大区/频道独立进程"仅限大内存机器 / 多机。

**网络平面收敛**（安全门槛 M0 / 红线 20）：admin、运维 API、`/internal/v1` 等内部接口默认绑定 **loopback / 内网**，不对公网暴露；仅客户端端口（Netty）与受控 `/api/v1` 按部署需对外。装配配置显式指定绑定地址，禁止默认 `0.0.0.0`。

### 4.3 拓扑

经典 MapleStory 布局（Login / World / Channel），v83 客户端天然支持"每个频道独立 IP:port"。

```
                    ┌─────────────────────────────┐
                    │    admin 控制台 / 运维 API     │
                    └─────────────┬───────────────┘
                                  │
                 ┌────────────────┴────────────────┐
                 │     world-coordinator（协调者）    │
                 │  共享状态真值 / 玩家定位表 / 调度    │
                 └───┬──────────┬──────────┬────────┘
                     │          │          │
              ┌──────┴───┐ ┌────┴────┐ ┌───┴──────┐
              │ 大区1 频道1 │ │ 大区1 频道2 │ │ 大区2 频道1 │ ...（进程）
              └──────────┘ └─────────┘ └──────────┘
                     └── 消息总线（频道间）──┘
```

- **频道 = 进程单元 = 世界分片**：一个频道进程拥有它那张图的一切（地图/怪物/掉落/连接它的玩家）。
- **一个进程可托管 N 个频道**（配置驱动）。代码不因"一进程一频道"还是"一进程多频道"而分叉——分发 = 配置 + 注册表。

### 4.4 频道间交互三机制

**铁律：共享状态绝不做"每个频道各持一份、靠同步保持一致"**（数据发散、互相覆盖的根源）。

| 类别 | 机制 | 内容 |
|---|---|---|
| **状态**（要持续、一致） | **单一属主**：共享状态只在一个地方持有真值（coordinator），频道需改则发消息、改完来读 | 公会、好友、排行、商店资金、活动数据 |
| **消息**（瞬间指令/通知） | **消息总线**：悄悄话=发目标频道、喇叭=广播、CC 请求=发目标频道。总线不存状态，只负责送达 | 悄悄话、喇叭、加好友、换频道、活动公告 |
| **玩家定位** | **定位表**（coordinator 维护）：player → 频道 | CC、悄悄话、加好友前先查"人在哪个频道" |

M0-M2 单进程阶段三机制全部进程内实现，但接口从第一天按"不假设进程内"设计。**接口先行、分布式后置。**

### 4.5 内部通信机制（进程间）

**跨进程通信是低频控制消息，不是高频实时。** 玩家实时状态（移动/战斗/地图内交互）只发生在所在频道进程内——单进程内存，不跨进程。真正跨进程的只有低频控制面：悄悄话/喇叭、加好友、组队/跨频道邀请、CC 换频道、公会/排行/商店资金变更。

**协议：不用 HTTP，用 Netty（TCP）长连接 + 自定义二进制帧。**

- **为什么不用 HTTP**：HTTP 是"请求-响应"问答模型，频道间通信本质是**事件投递**，模型对不上；HTTP 专用于对外 API（/internal、/api），内部进程通信不占用。WebSocket 对纯二进制投递是多余抽象，不采用。
- **零新依赖**：Netty 已是客户端协议栈，内部通信直接复用 `net-netty` 模块。
- **拓扑**：每个进程都是 Netty 客户端 + 服务端，与 coordinator 保持 TCP 长连接；coordinator 是中心路由器。

```
频道A ──TCP长连接── coordinator（中心路由+定位表） ──TCP长连接── 频道B
```

- **帧格式**：自定义二进制帧 `[帧头 | 消息类型 | 消息ID | 负载]`——复用 v83 包格式思路，或独立简单帧。
- **路由**：悄悄话 = 频道A → coordinator（查定位表）→ 转发频道B；广播（喇叭）= coordinator 群发所有频道。
- **可靠性在业务层**：帧只负责传输；**不丢 / 不重 / 有序**由业务层保证——持久化队列（投递成功才 ack，进程崩了重发）+ 接收方幂等去重（消息带全局 ID）+ 单一属主序号（同一逻辑流按序投递）。三者组合 = 语义上的恰好一次，是 CC 迁移不掉数据、不重复的核心。
- **单机多进程 = 分布式特例**：多进程在一台机器上，通信机制完全复用，只是走**本地回环**，延迟微秒级。回环更快，但不是更可靠——"进程崩了"照样发生，可靠性机制不能省。价值是**故障隔离**（一个频道崩了不影响其他），用内存换稳定性，只在 2C2G 之上的大内存机器用。**代码零改动、纯配置。**

**故障矩阵**（coordinator 崩 / 频道崩 / 网络分区——丢什么、保什么、RTO）：

| 故障 | 行为 | 丢什么 | 保什么 | RTO |
|---|---|---|---|---|
| **coordinator 进程崩** | 无状态重启；频道心跳超时 → 重连 → 重报 → 定位表自动重建（4.2） | 无（队列真值在 DB，重启后从 DB 重投未 ack 消息，接收方按全局 ID 去重） | 频道本地逻辑继续 | 秒级（重启 + 心跳重建） |
| **coordinator 机器宕机**（M6 跨机） | 整服跨频道通信/定位中断，直至该机恢复或人工迁移（coordinator 地址静态配置，故障转移会让频道指向死地址） | 跨频道功能 | 单频道可玩（频道本地逻辑不依赖 coordinator） | 人工介入（机器级 HA 见「待办/后续增强」） |
| **频道进程崩** | 该频道玩家掉线；在途跨频道消息由 coordinator 队列重投（发往已离线玩家的随玩家离线丢弃，可接受） | 该频道在线状态 | 其他频道不受影响（故障隔离） | 频道重启 |
| **网络分区**（频道 ↔ coordinator 失联） | 定位表过期，跨频道消息停摆；频道本地逻辑继续（无 split-brain——共享状态单一属主、频道不持副本） | 跨频道功能（降级为单频道可玩） | 一致性（无数据发散） | 分区恢复即自愈 |
### 4.6 分进程组件全图（配置中心 / 注册中心 / 插件 / AI / HTTP 归属）

#### 4.6.1 概念纠正：不是所有东西都是"进程"

分进程后，除了角色进程，还有一批常被误当成"第五类进程"的组件。它们不是进程，是**挂进角色进程的模块**：

| 组件 | 是不是进程 | 真相 |
|---|---|---|
| coordinator / channel | **是** | 分进程的意义所在，独立成进程 |
| login / admin / http / ai | 可选 | 低频、无状态，可并进管理进程 |
| **插件** | 不是 | 是"模块"，按部署作用域分发进对应进程（见 7.2） |
| **AI** | 不是 | 平台级模块，放管理进程 |
| **注册中心** | 不是 | coordinator 内建服务，不独立成组件 |
| **配置中心** | 不是 | DB 真值 + 版本号广播，管理进程提供服务 |
| **消息总线** | 不是 | 进程内 EventBus / 进程间 Netty 帧，同一接口 |

**决定归属的判据是"部署作用域"**：频道逻辑（战斗/任务）就近进频道进程，平台逻辑（AI/API）进管理进程，共享状态属主（公会/排行）在哪、其插件就进哪。

#### 4.6.2 完整拓扑（split 形态：频道隔离，管理侧合一）

```
┌───────────────────────────────────────────────────────┐
│ 管理进程（coordinator + login + admin + http + ai）       │
│                                                        │
│  ├─ coordinator：定位表 / 共享状态代理 / 消息路由          │
│  ├─ 内建 注册中心：channel 启动上报 host:port，心跳维护    │
│  ├─ 内建 配置中心：DB 真值 + 版本号，长连接推送变更         │
│  ├─ login：账号校验 / 选角                               │
│  ├─ admin：Web 控制台（替代 JavaFX）                      │
│  ├─ http-api：/internal/v1(官网) + /api/v1(第三方)       │
│  ├─ ai：LangChain4j Agent（平台插件）                     │
│  └─ 平台插件（AI / API 工具）                             │
└─────────┬──────────────────────┬────────────────────────┘
          │ TCP 长连接（注册+心跳+消息）  │
     ┌────┴──────┐            ┌─────┴─────┐
     │ 频道进程A  │   ......   │ 频道进程N  │
     │ channel   │            │ channel   │
     │ 频道插件   │            │ 频道插件   │
     │ (战斗/任务)│            │ (战斗/任务)│
     └───────────┘            └───────────┘
```

**进程总数 = `1 + 分片数`**（管理进程 1 个 + 每频道/每大区 1 个）。20 频道 → 21 个进程。login / admin / http / ai 全部并入管理进程——低频、无状态、只经 service 接口，为隔离它们单独付 JVM 常驻内存不值。

**"何时才值得分"判据**：单机多进程的价值是**故障隔离，不是性能**（4.5 已述）。20 频道在单机 16C32G 上若用 split 档 = 20 份 JVM 常驻开销；没有"一个频道崩了不能连累全服"的诉求，就 `--profile=single` 单进程抗满（现状即如此且没问题）。分进程留给**多机 / 多区**或**明确隔离诉求**时才划算。

#### 4.6.3 逐组件归属表

| 组件 | 分进程后进哪个进程 | 为什么 |
|---|---|---|
| 频道插件（战斗/任务） | **频道进程** | 游戏逻辑，同进程就近执行 |
| 大区插件（公会/事件） | **频道进程**（或管理进程） | 按数据属主定——公会状态真值在 coordinator 就进管理进程 |
| 平台插件（AI/API） | **管理进程** | 平台级，服务所有人 |
| **AI Agent + Tool** | **管理进程** | 只经 application service 接口，与频道内存隔离 |
| **第三方 HTTP（/api/v1）** | **管理进程** | 只读 DB + 经 service 接口，不能碰频道内存 |
| 官网转调（/internal/v1） | **管理进程** | 同上 |

#### 4.6.4 注册中心 = coordinator 内建轻量服务发现

- **真值方向**：channel → coordinator **单向上报**（频道ID、host:port、在线状态），coordinator 地址是静态配置。
- **心跳**：channel 定时心跳，超时标记下线；coordinator 崩了无状态重启 → channel 重连重新上报 → 定位表自动重建（见 4.2）。
- **消费方**：admin / http 需要知道频道在哪 → 问 coordinator；login 需要知道频道列表 → 问 coordinator。
- **不引入外部组件**：拓扑是星形（所有节点连 coordinator），coordinator 单点，2C2G 红线内建最省。

#### 4.6.5 配置中心 = DB 真值 + 版本号 + 长连接推送

- **真值在 DB**（`param_conf` 或独立配置表），管理进程内配置服务读它、持有版本号。
- **各进程启动拉取一次**，之后经 TCP 长连接**订阅变更**（coordinator 广播版本号变更 → 各进程重读）。
- **变更链路**：admin 控制台改配置 → 写 DB → 版本号 +1 → 广播 → 各频道重读。这把 5.2 L1 的"配置服务 + 版本号 + EventBus 广播"从进程内升级为跨进程。
- **为什么必须有**：分进程后 20 个频道进程各持一份配置，改一处若不同步，各频道行为即分叉（倍率/开关不一致）。配置中心存在的意义就是消灭这个分叉。

#### 4.6.6 第三方 HTTP 跨进程取数（三路）

分进程后 http-api 在管理进程、游戏数据在频道进程内存，HTTP 本就禁止直踩游戏内存，分进程**把这条约束物理化了**——两进程不共享内存，HTTP 想碰频道数据都碰不到，只能走以下三路：

| 路 | 适用数据 | 机制 |
|---|---|---|
| **① 查 DB**（主路） | 市场记录、商店快照、角色存档 | 数据本就在 DB，直接查。分进程后主路 |
| **② 经 service 接口** | 事务性操作（改密、封禁） | 管理进程内 service → 涉及频道在线状态 → **RPC 到频道进程**（复用消息总线帧） |
| **③ 事件驱动快照** | 热数据需对外（避免高频轮询） | 频道进程把状态变更事件推给管理进程维护**只读镜像**，HTTP 读镜像 |

> **镜像纪律**：第三路的镜像是**单向、只读**——频道进程推、管理进程存、HTTP 读，真值仍在频道进程。**禁止经镜像回写**（否则违反"共享状态单一属主"铁律，产生双写源）。写操作一律走第②路经 service 接口 RPC 到频道进程。

### 4.7 CC 迁移（核心原语，一机制两用）

- **功能性**：玩家换频道 = 老频道 flush 状态 → 目标频道加载 → 客户端重连（v83 客户端换频道本来就是 loading 界面，玩家熟悉）。
- **兜底性**：升级前把玩家挪到别的频道 → 重启 → CC 回来。玩家视角只是"换了一次频道"。

---

## 五、热更新与存档安全（核心设计）

### 5.1 为什么进程内真无感热重载可行

**进程内真无感热重载可行**：客户端限制（断线只发生在对端进程没了 / 心跳超时）只在换进程时成立，进程内重载不碰 TCP、耗时小于心跳超时，客户端无感知；Java 的 hot-swap 只能改方法体——但可重载代码隔离在**模块 classloader**，状态不进该 classloader（在稳定层/DB），替换 = 新 classloader + 新实例 + 重新绑定接口，无旧类实例冲突；架构上的"双进程+实时同步"是换进程路线的解法，状态分离后单进程内模块替换不需要。**不是迁移对象，是对象根本不进可重载代码。**

**加分项**：游戏 tick 单线程，换点干净——tick 帧边界暂停、卸载 handler、加载新 classloader、重注册、恢复，无并发执行中的逻辑，不用排空。

### 5.2 分层热更新

| 层 | 做什么 | 机制 | 风险 |
|---|---|---|---|
| **L1 配置** | 倍率/开关/参数热改 | 配置服务 + 版本号 + EventBus 广播，订阅者重读。**单进程为进程内实现；分进程为跨进程：DB 真值 + 版本号 + TCP 长连接推送**（见 4.6.5） | 低 |
| **L2 脚本** | NPC/任务/事件 JS 热重载 | GraalVM 重读文件即生效；统一 ScriptManager + reload 入口 | 低 |
| **L3 逻辑/插件** | Java 逻辑热替换（进程内，**无感**） | 模块 classloader 替换 + 按实体渐进重载（见 5.3） | 中 |
| **L4 进程兜底** | 进程必死（换 JDK/崩溃/泄漏） | **例外场景**，只保不丢数据：DRAINING + 增量 FLUSH + 重启 + 恢复 | 低-中 |

### 5.3 按实体渐进重载（防复制 bug 的关键设计）

**绝不做"全服同步原子重载"**——交易等跨 tick 操作在切换瞬间进行，新旧逻辑读到"锁定中的背包"即丢锁，产生"背包没扣、交易成功"的 day0 级 bug。

- **重载原子单元 = 单个玩家/频道**，不是"全服某时刻"。
- 每个实体只在**无在途操作的安全点**切换：丢物是单操作（单 tick 内删 item）天然安全；交易是长操作，**等其自然结束**（秒级）或**显式中断 + 回滚**（交易天然可回滚：双方背包归位，玩家看到"交易被取消"）。
- 玩家可感知的极限是"交易被中断"，**不是"东西没了/多出来了"**。
- 逻辑越无状态（从索引读 → 计算 → 写回，不持有跨操作状态），安全点越密，切换越干净。**逻辑无状态化是热重载成立且不出错的前提。**
- 实现细节：重载后旧逻辑的**迟到写操作**要能识别并丢弃/重放，即写路径带"版本门"。**契约已在 M0/M1 定稿**（core 的 `org.gms.hotreload.versioned` 包：`VersionGate` 接口 + `Versioned` 写操作标记 + `DefaultVersionGate` 实现），M2 所有写入口从第一天按版本门感知编写，M3 只做机制装配，不 retro-fit。

### 5.4 存档安全与主动重开

**参考项目痛点**：异步保存队列会掉档；主动重开要么掉档、要么全量落盘等几分钟停不下来。**本架构不继承这两点。**

**核心纠正：主动重开 ≠ 全量落盘，= 快重启 + 恢复**（"重开快 + 恢复快"，不是"关得慢 + 落盘全"）：

| 路径 | 做法 | 效果 |
|---|---|---|
| **路径 A（首选）** | 主动升级若只改逻辑/配置 → **走 L3 进程内热重载，不停进程** | 消灭"停服等待" |
| **路径 B** | 必须换进程（换 JDK/大版本）→ **秒级重开 + 上下文恢复**：不要求全量落盘，DRAINING 只排空在途操作 + 单写执行器排空，重启后从 DB 恢复，玩家"闪一下"回来 | 秒级 + 不丢 |
| **路径 C（兜底）** | 确实要落盘 → **只 FLUSH 脏数据**（自上次落盘后变更的），不做全量 | 落盘时间 = 脏数据量 |

**主动重开三件套**：**增量 FLUSH**（不靠 WAL）+ **WZ 文件缓存**（省重解析）+ **上下文恢复**（原地复活）。**主动重开不需要 WAL**——你能控制关停时序，优雅落盘即可；WAL 只服务随机崩溃。

**随机崩溃**（kill -9 / OOM）：允许丢最近几秒（见 5.5 轻量模式），恢复 = 加载 DB。若需零丢失，启用可靠模式。

### 5.5 WAL（按需组件，不是必装）

**snapshot 与 WAL 是同一事物的两种策略**（WAL = 连续小快照，快照 = 一次性大 WAL），**只留 WAL**。WAL 是按需组件——一套代码、按需启用：

| 模式 | WAL | 崩溃丢失 | 用途 |
|---|---|---|---|
| **轻量模式**（默认） | 不启用，恢复=加载 DB | 丢最近几秒 | standalone / 2C2G |
| **可靠模式** | WAL 是变更日志 + 恢复源 | 零丢失 | 运维要求零丢失时启用 |

- **同一判据**：接口 = `变更 → 持久化`，内存实现 = 直接改内存；WAL 实现 = 先落 WAL 再改内存。切实现即切模式。
- **WAL 重量可调，不默认"重"**：每条 fsync（零丢失但拖慢）→ 批量 fsync（丢最近几秒、影响小）→ 纯内存（进程死全丢）。2C2G 用批量 fsync。
- **可靠模式三性质**：先写 WAL 再改内存（或同级事务）；记录原子落盘（批量 fsync 以控 2C2G 磁盘 I/O）；重放幂等（逻辑系统重放不得重复扣血/给经验）。
- **checkpoint**：定期把 WAL 合并进 DB 并截断（控体积，避免恢复过慢）。
- **审计**：WAL 记录带元信息（谁、何时、什么），顺带实现，无需单独审计层。
- **WAL 版本化**：WAL 事件带数据格式版本号；migration 变更 schema 时旧 WAL 也要迁移（否则重放时新逻辑读旧数据读炸）。

---

## 六、数据层（持久化 + 查询层）

### 6.1 定位

**内存态是权威，DB 只是持久化 + 查询层。** DB 的两类活：角色/物品存档写入（低频、大块）+ 管理/API 查询（低频、报表类）。

### 6.2 选型

| 项 | 决定 |
|---|---|
| 关系库 | **按需三档**：SQLite（低配 standalone 默认）/ PostgreSQL（大服/分布式）/ MySQL 8+、MariaDB（兼容切换） |
| ORM | **MyBatis-Flex**（一份代码跑三库，常规 CRUD 全覆盖） |
| 复杂原生 SQL | 用"两库公共子集"语法写（SELECT/JOIN/CTE/窗口等本就通用） |
| 方言差异点 | 自增 / upsert / 布尔 / 时间函数 / 字符串函数 → 集中进 **`db-dialect`** 模块，按配置选实现 |
| 迁移 | **自研迁移器**（M0 决策），每库独立版本表，启动时跑 |
| seed | 基础数据随版本发布，与逻辑同版本（migration 管结构、seed 管内容） |

**拓扑 → DB 映射**（M0 起生效，防误用低配库跑大服基线）：

| 拓扑档 | 资源基线 | 数据库 |
|---|---|---|
| `standalone` | 2C2G / 1C1G（最低配） | **SQLite**（嵌入式，数据库常驻内存归零） |
| `single` | 16C32G（性能极致档） | **PG / MySQL** |

**SQLite 上限 ≈ 数百人在线**（受单写连接约束）；**2000+ 绝不上 SQLite**——单写连接串行化是吞吐天花板。`single` 档低配机器临时跑 SQLite 可以，但 16C32G 大服基线必须配 PG/MySQL。由装配配置决定，代码不写死（接口不变换实现，铁律 1）。

**为什么 100 人用 SQLite 够（搜索核证）**：WAL 下实测 70,000 reads/s + 3,600 writes/s；读吞吐 2 倍于 MySQL、p99 读延迟低 19 倍。**SQLite 的极限是"持续 >10 并发写/秒"而非"人数"**——本架构 DB 层非热路径（热路径在内存），写 = 单写执行器（异步排队）+ 定期 flush + 下线存档，低频批量，天然避开软肋。嵌入式零常驻服务，是"极致省"的正解。**唯一边界：SQLite 单文件不能跨进程共享**，故单机多进程 / 分布式必须换真库——套铁律 1，接口不变、换实现即可。

**SQLite 并发写处理**（低配默认档，**三件套做对即稳**）：
- **① WAL 模式**：读写并行，读者永不阻塞。
- **② 单写连接**：所有 DB 写经一个后台写线程排队串行。游戏逻辑本就单线程 + 全写路径统一入口，从构造上消灭 SQLITE_BUSY。**此队列在 DRAINING 阶段必须排空**（主动重开不丢档的前提）。
- **③ busy_timeout**（~100ms）：瞬态互斥等待而非报错。短事务：写事务内不碰网络 I/O，锁持有毫秒级。
- **逃生舱已在设计内**：写负载真超上限时切 PG 即解——同接口、改配置。这正是"按需三档"而非"锁死 SQLite"。

### 6.3 方言策略

> 业务代码不允许出现**裸的方言差异**——出现就进 `db-dialect`。

绝大多数 SQL（SELECT/JOIN/GROUP BY/ORDER BY/LIMIT/子查询/CTE/窗口函数）两库通用，直接写；少数方言点（upsert/自增/布尔/时间/字符串函数）封装进 `db-dialect`，方法级（`nextId()` / `upsert()` / `toTime()`…），PG / MySQL / **SQLite** 各一个实现，运行时按配置选。MariaDB 归 MySQL 系，随 ORM 覆盖。

**SQL 能力矩阵**（M0 定义、M2 复杂 SQL 准入清单，违反即红线）：

| 能力 | 判定 |
|---|---|
| 参数化查询 | **强制**：全库经 MyBatis-Flex `#{}`；禁止 `${}` 与字符串拼接 SQL（安全红线 18，CI 扫描拦截，见 `SqlInjectionScanTest`） |
| 通用 SQL（SELECT/JOIN/GROUP BY/ORDER BY/LIMIT/子查询/CTE/窗口函数） | **允许**：两库公共子集直接写 |
| 方言点（upsert/自增/布尔/时间/字符串函数） | **禁止裸写**：进 `db-dialect` 方法级封装（红线 14） |
| 存储过程 / 触发器 | **禁止**：SQLite 不支持，非公共子集 |
| 复杂原生 SQL 兜底（红线 7：Character 存档 60 列联写） | **允许**：必须参数化，禁止拼接 |

### 6.4 WZ / 脚本数据源定位

> WZ 与脚本不受 `twinkle.service.language` 控制，也不自动拼接语言后缀。部署者通过两个显式路径选择实际数据；服务端不会猜测、合并或回退到其他目录。

- **WZ 位置**：`twinkle.wz.path` 配置直接指定 WZ 数据目录（如 `./wz/`）。wz-provider 从该目录读取，不做语言目录，单份数据。
- **脚本位置**：`twinkle.script.path` 配置直接指定脚本目录（如 `./scripts/`）。domain-script 只读取该目录。
- **未指定 / 读不到**：启动时打印明确错误（日志红线 9），指出配置项、实际路径与缺失内容，进程以可诊断状态退出或告警——**不做静默回退**。
- **服务端语言互不干预**：`twinkle.service.language` 只影响 Java 后台自身的人类可读输出；不会改变 WZ/脚本目录。
- **客户端编码**：v83 中服线协议字符串仍按 GBK 编解码；charset 与 Java 后台 i18n 是两套独立机制。

---

## 七、插件系统（VSCode 式）

### 7.1 模型

VSCode 模型的本质不是"能加载 jar"，而是**平台只暴露贡献点、SDK 版本化、隔离**：

| 特性 | 游戏服对应 |
|---|---|
| 贡献点（命令/菜单/视图/语言服务） | **贡献点 = PacketHandler / HTTP 路由 / 定时任务 / 事件监听 / Script 命名空间 / AI Tool / 游戏逻辑系统** |
| SDK 版本化 | **`plugin-api` 模块**，插件依赖它，版本变则 API 升 |
| 隔离 | 插件 classloader 隔离 |

### 7.2 关键结论

1. **插件系统 = 热重载系统**：同一套 classloader + 贡献点机制，不重复造。
2. **部署作用域**：频道插件（战斗/任务）、大区插件（公会/事件）、平台插件（AI/API）。分发后插件按进程类型部署。
3. **信任边界 = 全权**：插件可访问一切数据，但**通过接口**操作（可替换层不得引用游戏对象具体类——见第三节）。

### 7.3 核心约束

- 插件声明式注册（manifest）。
- 插件 classloader 的**可见性边界**：绝不能加载/遮蔽 Character/MapleMap 等稳定层类，否则 CCE。
- 贡献点（PacketHandler/HTTP/Script/Task/Event）**从第一天版本化**——它们一旦可装卸即成为兼容面。

---

## 八、AI 集成

- LangChain4j `AiServices` 声明式 Agent + `@Tool` 注解，多工具调用靠模型原生 function calling 自动循环。
- 流式 `TokenStream` + 工具调用原生合一。
- 场景：AI 报表 / AI 数据统计 / 多工具调用 / 每日总结（`@Scheduled`）/ 玩家游戏内 `@gm` 值班入口。
- 结构化输出：返回 POJO/Enum/JSON 自动解析；RAG 全套备查 WZ/游戏知识库。
- **AI 工具不得直踩游戏内存对象**（同 HTTP 约束），只经 application service 接口。计费/记忆/配置落 SQLite（复用现有 Dao 设计；低配模式下 SQLite 本即主库）。
- Agent 默认关闭；真实模型密钥只经环境变量注入。玩家入口只读本人数据，工具调用写权威审计；外部模型在独立线程池运行，不得阻塞 Netty/游戏 tick。

---

## 九、性能红线（2C2G 硬指标）

> **2C2G 能跑，是硬指标，不是愿望。** 重构前的（2C2G 单进程）能跑，重构后必须也能跑。每个组件必须回答："这玩意儿在 2C2G 上待得住吗？"

### 9.1 内存预算（2G 总账）

```
2G 总内存预算（示例，M0 校准）：
  堆 1G（游戏对象、状态、业务）
  堆外 256M（Netty 直接内存、WZ 缓存）
  原生内存 512M（GraalVM JS 引擎、JIT、语言运行时）
  系统/OS 余量 256M
```

**"能跑"不是重构完再测，是预算从一开始就写死。**

> 低配 standalone 用 **SQLite** 时，数据库常驻内存归零（嵌入式、进程内），2G 预算更宽裕，2C2G 乃至 1C1G 均可评估。

**JDK 内存调优**（GraalVM CE for JDK 21，基于 HotSpot）：AppCDS + SerialGC + 收紧堆，实测可将非堆占用从 ~600M 压到 ~420M。此项 M0 校准。

### 9.2 逐组件 2C2G 判定

逐组件判定（Netty 留 / MyBatis-Flex+SQLite 留 / Micronaut 留盯 / GraalVM JS 盯 / WAL 按需 / AI 按需 / 独立 coordinator 降级为进程内模块）见 2.1 选型表与第十节红线，不重复列表。

### 9.3 CPU 与启动预算

- 2C2G 只有 2 核。WZ 加载、脚本预编译、JVM 预热在 2 核上偏慢。
- **WZ 缓存文件序列化**（预编译到磁盘，启动读文件，省重解析）——秒级重开在 2C2G 上的关键。
- 启动耗时基线写入 M0 验收项。

---

## 十、关键约束（红线）

1. **v83 协议字节级兼容**：`net.encryption` + `RecvOpcode` 原样移植。
2. **MySQL `newmaple` 库兼容**：表结构不改，扩展走 migration `ADD COLUMN`（禁止单条 ALTER 串接多列）。
3. **存档格式兼容**：`characters` ~60 列 + keymap/skills/queststatus/inventory 结构不变。
4. **游戏对象不进容器**：Character/MapleMap/Item 手动 new，容器只管基础设施 + Service。
5. **数据库按需三档**：低配 standalone 用 SQLite；大服/分布式用 PG；MySQL/MariaDB 兼容切换。方言差异点进 `db-dialect`。
6. **HTTP 与游戏 Netty 隔离 EventLoop**：第三方 API 流量不挤占游戏 tick 线程。
7. **Character 存档复杂联写保留原生 SQL 兜底**（60 列联写不强求 ORM 级联）。
8. `accounts.banned` 只有值 `1` 明确表示已封禁，查询未封禁必须用 `banned <> 1`（兼容 NULL 语义）。
9. 日志用 log4j2 `log.error("描述", e)`，禁用 `e.printStackTrace()`。
10. 使用全限定类名必须 import 后用短名（除非类名冲突）。
11. **可替换层不得引用稳定层具体类**（接口或数据投影，防 CCE）。
12. **可替换层不得持有跨操作状态**（逻辑无状态化 = 热重载安全前提）。
13. **贡献点从第一天版本化**（可装卸即兼容面）。
14. **业务代码不允许出现裸的方言差异**（出现即进 `db-dialect`）。
15. **2C2G = 强制单进程，禁止多 JVM**："大区/频道独立进程"仅限大内存机器 / 多机。多 JVM 常驻开销每多开一个整体翻倍。
16. **JDK 锁定 GraalVM CE for JDK 21**：原生内置 JVMCI + GraalVM JS，GraalJS 全速无需额外开关；不回退 stock Temurin。
17. **主动重开 ≠ 全量落盘**：只 FLUSH 脏数据（增量），不做全量 flush；"等几分钟停服"是旧架构全量落盘的产物，本架构不继承。

---

## 十一、路线图

| 里程碑 | 交付 | 验证 |
|---|---|---|
| **M0** | 骨架 + Micronaut + 多模块 + MyBatis-Flex 连 SQLite（低配默认）与 PG（大服）双验证 + 配置门面 + **自研迁移器**（替代 Flyway，社区版不支持 SQLite）+ **热更新地基（接口化/事件化/模块 classloader/RestartCoordinator 状态机）** + **状态/逻辑分离边界** + **2C2G 内存/启动预算基线** + **parity 测试基建** + **GraalVM CE for JDK 21 + GraalJS 全速验证** + **SQLite 100 人负载实测（写延迟/锁冲突/峰值内存）** + **增量 FLUSH 重开实测（秒级 + 不丢档）** | 能启动、连 SQLite、param_conf 热改、2C2G 跑通、GraalJS 峰值验证、WZ 文件缓存 |
| **M1** | 协议 + Netty + HandlerRegistry + NetworkSession | 客户端登录、进图 |
| **M2** | 游戏逻辑**重写**（状态/逻辑分离），参考项目作 parity 真值（录包回放 / 双跑对照） | 行为对齐参考项目 |
| **M3** | HTTP 重做（/internal + /api）+ LangChain4j AI + **按实体渐进重载** | API 可调、AI 流式+工具可用、重载无复制 bug |
| **M4** | 插件框架 + 热更新 L1-L4 全通 + **频道间三机制** | 插件可装卸、重开不丢档、悄悄话/CC 走三机制 |
| **M5** | Web 控制台 + 迁移 + 上线切换 | 控制台、单库迁移、切换 |
| **M6** | **分布式**（大区/频道独立进程、world-coordinator、CC 迁移、消息总线） | 多机部署、玩家换频道、升级滚动 |

---

## 十二、可观测性（Observability）

> 硬约束：可观测性是 **M0 第一天就进验收的 P0 项**，不是上线后补。动态扩展的前提是知道瓶颈在哪；稳定性的前提是知道何时降级。三大支柱 Metrics / Tracing / Logging，外加 Health 与 Alerting。2C2G 红线内不引入独立 Agent 常驻——Micrometer 进程内注册表 + Prometheus 服务端拉模式。

### 12.1 Metrics（指标）
- **采集**：Micrometer 核心 + Prometheus Registry（Micronaut 原生集成）；Prometheus 服务端独立部署，不在 2C2G 进程内。
- **核心 SLI（必须暴露）**：
  - 游戏 tick：`tick` 时间预算 p50/p95/p99（目标 < 9.1 预算阈值）、tick 掉帧计数
  - 存档：增量 FLUSH 耗时、写队列深度（SQLite 单写连接积压）、存档延迟（下线→落盘）
  - 登录：登录延迟 p95、握手失败率、登录端口限流命中数
  - 在线：当前在线人数、各频道人数分布、CC 迁移次数/失败数
  - 跨进程：消息总线未 ack 积压、coordinator 心跳超时次数、RPC 超时/失败率
  - JVM/资源：堆/堆外/原生内存（对照 9.1 预算）、GC 暂停、GraalVM JS 引擎内存、CPU、Netty 直接内存、WZ 缓存命中率
  - 业务语义：交易/商店资金变更次数（单一属主账本）、好友/公会操作延迟

### 12.2 Tracing（链路）
- 跨进程消息（coordinator 路由、CC 迁移、RPC 到频道）带 `traceId`，复用消息全局 ID 体系（4.5）。
- 低频控制面（悄悄话/喇叭/跨频道邀请）链路可视化，定位"消息丢了还是慢了"。
- 游戏 tick 内部不强制全链路（高频热路径开销大），仅在跨进程边界埋点。

### 12.3 Logging（日志）
- 结构化日志（log4j2 结构化布局），统一字段：`ts / level / traceId / channelId / playerId(脱敏) / module`。
- 继承红线 9（禁 `printStackTrace`）；**禁打印明文凭证**（密码/token/API Key/SQL 参数）——接 security 红线 23。
- 分级：tick 错误→error；热更新/重载事件→info（含版本号）；性能预算突破→warn（触发告警）。

### 12.4 Health & Alerting（健康与告警）
- 健康检查（管理进程暴露）：`liveness`（进程活）/ `readiness`（依赖就绪：DB 连接、coordinator 连接、WZ 加载完成）。
- 告警基线（示例，M0 校准）：tick p99 > 预算持续 30s → warn；SQLite 写队列深度 > N → warn；coordinator 心跳超时 → critical（关联 4.6.4）；在线人数突降 > X% → critical；原生内存 > 9.1 预算 90% → critical。
- 告警统一经管理进程出口（不直连外部，防配置泄露）。

### 12.5 降级联动（与稳定性挂钩）
- 可观测性是降级决策的"眼睛"：DB 慢 → 提高 flush 间隔 + 降非关键查询；coordinator 失联 → 单频道可玩、跨频道暂停（4.5/4.6.4 降级声明的数据支撑）；GraalVM JS OOM 风险 → 限脚本并发。
- 降级状态必须可观测：暴露 `twinkle_degraded{mode="..."}`，避免静默降级。

### 12.6 M0 验收项（追加）
- [ ] Micrometer + Prometheus 接入，`/metrics` 暴露 SLI 子集（至少：tick p99、在线人数、写队列深度、存档延迟）
- [ ] 结构化日志格式落地 + 脱敏校验
- [ ] 健康检查端点（liveness/readiness）
- [ ] 至少 2 条告警规则（tick 预算突破、coordinator 心跳超时）

---

## 待办 / 后续增强（不阻塞主线）

- **分布式完整拓扑**（world-coordinator 独立进程、网络总线、CC 迁移自动化）——M6
- **跨进程 WAL 同步**（双写 WAL 到另一台，真零丢失）——分布式能力，另案
