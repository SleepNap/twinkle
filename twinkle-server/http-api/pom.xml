<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.gms</groupId>
        <artifactId>twinkle-server</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>twinkle-http-api</artifactId>
    <name>twinkle-http-api</name>
    <description>Micronaut Controller（/internal/v1 官网 + /api/v1 第三方，限流+版本化）。管理侧</description>

    <dependencies>
        <!-- Micronaut HTTP Server（Netty） -->
        <dependency>
            <groupId>io.micronaut</groupId>
            <artifactId>micronaut-http-server-netty</artifactId>
        </dependency>
        <!-- HTTP Controller/DTO 的机器可读契约注解；规范由编译期 micronaut-openapi 生成。 -->
        <dependency>
            <groupId>io.swagger.core.v3</groupId>
            <artifactId>swagger-annotations-jakarta</artifactId>
            <version>${swagger.annotations.version}</version>
            <scope>provided</scope>
        </dependency>
        <!-- 严格捕获公共 DTO 的未知 JSON 字段，继续由版本协议校验器统一报错。 -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
        </dependency>
        <!-- 限流：Bucket4j -->
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j_jdk17-core</artifactId>
            <version>${bucket4j.version}</version>
        </dependency>
        <dependency>
            <groupId>org.gms</groupId>
            <artifactId>twinkle-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.gms</groupId>
            <artifactId>twinkle-data</artifactId>
        </dependency>
        <!-- 管理员登录密码散列：jBCrypt（安全红线，不落明文） -->
        <dependency>
            <groupId>org.mindrot</groupId>
            <artifactId>jbcrypt</artifactId>
            <version>0.4</version>
        </dependency>
        <!-- 管理侧禁止依赖 domain-game -->
    </dependencies>
</project>
