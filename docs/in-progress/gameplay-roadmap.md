# 游戏功能推进（2026-09-06）

本路线以玩家实际操作为入口。历史 M0–M6 的完成状态代表基础架构里程碑，不代表所有 v83 玩法已经可用。打包发布脚本已按要求删除，当前优先完善游戏功能。

按 2026-09-06 最新安排：先连续迁移封包，真实客户端联调及完整场景验收后续统一进行，不作为每批迁移的前置条件。迁移期间保留编译、相关自动回归和架构检查。

## 参考边界

北斗作为行为和协议字段的核对来源，不复制其 Java 实现、脚本、SQL、商品列表或资源文件。新增实现使用 Twinkle 的角色状态、版本门、频道运行态和封包写入器。测试 WZ XML 独立构造，商店初始配置独立编写。

本次核对范围：地图切换、NPC、背包更新、掉落、商店、任务、AP/SP、普通自身 Buff、组队消息的字段顺序。引用位置为北斗对应接收 Handler、PacketCreator 以及 WZ 的字段含义；协议相关类已注明来源。开发时外部 WZ 仍通过 `twinkle.wz.path` 读取，不将参考仓库内容带入本仓库。

## 本轮已实现的服务端范围

以下“已实现”指代码已接入频道并有自动回归验证；尚未完成真实 v83 客户端联调。

| 功能 | 本轮落地 | 当前边界 |
|---|---|---|
| 多人地图与动作 | 进图确认后双向生成角色、离图/换频道/断线移除、外观和现金覆盖、位置/姿态、基础表情、背包椅子、同屏 Buff | 无宠物、公会徽章、戒指特效、摊位；无现金表情、固定地图座位和椅子恢复效果 |
| 操作设置与详情 | 90 键位修改、最多五个技能宏、八格快捷栏、登录回发、角色主表原子存档；同图角色基本详情和勋章 | 无宠物自动用药设置；详情中的公会/联盟/宠物/骑乘/愿望清单/图鉴待对应状态接入 |
| 换地图与传送门 | 服务端选目标及落点、距离校验、进图对象发送、死亡回城、NPC 脚本宿主真实换图 | 普通 WZ 目标门；脚本门、地图进入脚本尚未接通 |
| 背包与物品 | 正槽移动、拆分、合并、交换、普通恢复药、完整实例和手动金币丢弃/拾取、掉落过期清理 | 不含穿脱装备、怪物掉落、宠物自动拾取 |
| NPC 商店 | 真实地图 NPC 对象、服务端商品配置、买卖、金额及容量预检、换图后旧商店失效 | 普通堆叠商品；装备商品、飞镖/子弹充值待做 |
| AP/SP | 手动和自动分配四维 AP，SP 扣点、职业分支、前置技能、技能等级上限、龙神 SP 编码 | HP/MP AP、新手专用 SP、技能书和升级/转职发点待做 |
| 基础任务 | 接取、放弃、完成、NPC/等级/职业/物品/前置任务/击杀检查，物品和金钱奖励原子提交，击杀进度回包 | 不含脚本任务、随机/选择奖励、装备奖励、时间条件；未知条件或奖励拒绝执行 |
| 自身 Buff | 圣甲术 `1001003`、魔法盾甲 `2001003`、集中术 `3001003` 的 WZ 等级读取、扣蓝、重施、取消、死亡及到期清理 | 仅这三个普通自身增益；完整战斗属性计算、范围增益、召唤和跨频道 Buff 保留待做 |
| 同频道组队 | 创建、邀请、拒绝、加入、退出、解散、踢人、转交队长、地图/等级刷新、离线清理、队伍聊天 | 临时频道队伍，最多六人；离开频道即退出，队长离线解散；无经验分配、跨频道保队、队伍门 |

同时修复两个基础问题：各频道不再共用 WZ 缓存中的地图运行对象；NPC 对话不再把对象 ID 当成模板 ID。药水改为精确消耗请求槽位，并用正确的 HP/MP 状态包响应。普通奖励/购买的物品不会堆进带不同期限、主人等属性的旧实例。

商店配置：`twinkle.gameplay.shop-path` 可指向自有 properties 文件，格式为 `NPC模板ID=物品ID:单价,物品ID:单价`。未配置时使用 `channel/src/main/resources/gameplay/shops.properties`，当前只有独立编写的药水示例店。

## 接下来按这条路线继续

1. **装备穿脱与属性**：读取 Character.wz 装备定义，校验职业/等级/部位，装备交换、基础属性与装备属性汇总。
2. **战斗与死亡**：区分物理/魔法技能，校验技能等级、消耗、目标和命中次数；怪物伤害、受伤、死亡、复活、异常状态统一结算。
3. **击杀收益与升级**：怪物经验/金币/物品掉落配置，掉落归属，经验阈值、升级和 AP/SP 发放。当前攻击入口仍有徒手伤害简化，任务经验只记数值，不能标为完整升级链路。
4. **转职和技能成长**：新手技能点、转职任务、技能书、洗点、更多主动技能与 Buff 的实际战斗效果。
5. **地图及任务脚本**：自研 portal/quest 宿主，地图进入/离开触发，限时地图，条件门，选择奖励和重复任务。
6. **社交补全**：好友请求/确认/完整列表、好友上下线、组队 HP/经验共享、跨频道队伍属主与恢复、公会。
7. **更多长期玩法**：仓库、玩家商店、宠物、掉线重连、活动/副本与每日任务。按上述基础闭环完成后推进。

每项先明确服务端规则与协议，再实现独立代码与回归。客户端操作结果留到统一验证阶段验收，不以新增 Handler 数量判断玩法完成。

## 验证与验收

### 第三批封包：操作设置、详情、队伍聊天和金币

| 接收 | 对应发送/存档 | 本批范围 |
|---|---|---|
| `CHANGE_KEYMAP 0x87` | `KEYMAP` | 90 个可配置键位，技能绑定检查已学习技能；重复键、越界或任一非法项整批拒绝 |
| `SKILL_MACRO 0x6E` | `MACRO_SYS_DATA_INIT` | 最多五组、每组三技能；名称/技能校验，删除宏时整体替换，保留其他设置 |
| `CHANGE_QUICKSLOT 0xB7` | `QUICKSLOT_INIT` | 八个 int 键位，拒绝窄化截断；未自定义时发送客户端默认快捷栏标志 |
| `CHAR_INFO_REQUEST 0x61` | `CHAR_INFO` | 同地图有效会话的等级、职业、名声、已穿戴勋章和已完成勋章任务 |
| `MULTI_CHAT 0x77` | `MULTICHAT` | 同频道队伍聊天；收件人来自服务端队伍，客户端伪造名单无效 |
| `MESO_DROP 0x5E` | 余额更新、掉落生成/移除 | 单次 10–50000，余额/版本校验，拾取原子认领，余额上限失败保留掉落；复用两分钟过期 |

字段含义核对来源为北斗相应 Handler、`PacketCreator`、`QuickslotBinding`。初始九个基础按键布局独立选择，不复制参考项目的配置列表。操作设置保存为自有版本化快照，经过 `PlayerCharacterAssembler` 与角色事务存档一起保存和加载。

按开发期数据库规则，在三个方言的 V2 主表定义中加入可空 `control_settings` 字段，没有改动用户现有数据库。后续统一验证前，旧开发库需按项目约定重建；新库直接执行完整迁移即可。未设置该字段的角色使用基础操作布局。

本批新增六项回归，扩展队伍聊天与数据库回滚测试；定向检查覆盖设置整体拒绝/重登恢复、宏与快捷栏编码、跨地图详情拒绝、金币并发拾取及余额溢出。完整客户端场景统一验证后置。

### 第二批封包：同屏角色与动作

协议布局核对北斗的 `PacketCreator`、`AbstractMovementPacketHandler` 和表情/椅子 Handler；只读取字段含义与长度，以下编码、状态管理和测试独立实现。

| 方向 | Opcode | 已接入行为 |
|---|---|---|
| 收 | `FACE_EXPRESSION 0x33` | 基础表情 1–7；广播给同图其他角色 |
| 收 | `USE_CHAIR 0x2B` / `CANCEL_CHAIR 0x2A` | 校验 WZ 存在、持有有效椅子实例；坐下/起身，死亡或椅子移出后清理 |
| 收/发修正 | `MOVE_PLAYER 0x29 / 0xB9` | 全流验证、13 字节绝对移动片段、有符号坐标、姿态和立足点同步；相对速度不再累加成位移；版本拒绝不广播 |
| 发 | `SPAWN_PLAYER 0xA0` / `REMOVE_PLAYER_FROM_MAP 0xA1` | 等待加载完成；同图双向同步、重复确认幂等、跨地图实例隔离、迟到旧连接不删除新角色 |
| 发 | `FACIAL_EXPRESSION 0xC1` / `SHOW_CHAIR 0xC4` / `CANCEL_CHAIR 0xCD` | 表情与椅子显示、本人起身确认 |
| 发 | `SHOW_FOREIGN_EFFECT 0xC6` / `GIVE_FOREIGN_BUFF 0xC7` / `CANCEL_FOREIGN_BUFF 0xC8` | 已支持的三个自身 Buff 施放动画、远程属性及取消；后来进图补发尚未到期的 Buff |

外观共用编码器已修正：普通武器位于装备槽 11，现金武器单独编码；现金服饰覆盖对应普通槽，原装备进入遮盖列表。未实现的特殊角色数据使用空段，不把公会、宠物、婚戒、摊位标为完成。

新增 13 项自动回归，另扩展既有 Buff 测试；涵盖生成报文偏移、并发进图、连接替换、换图顺序、Buff 补发、椅子持有/期限、移动截断和版本拒绝。移动解析验证全部 23 类已知片段及每个截断边界。真实客户端录包对照仍待完成。

新增自动回归覆盖：

- 非法/远距离传送不改变地图；频道地图运行态相互隔离。
- 并发拾取只能成功一次；背包满、地图不符时掉落仍可被合法拾取。
- 指定槽位用药；物品拆分/交换保留实例属性；购买金额溢出及容量不足不扣款。
- 商店会话、报价、NPC 距离校验。
- 任务条件、服务端击杀进度、满背包失败回滚、重复完成不重复奖励。
- AP 超额分配及跨职业 SP 拒绝；自动 AP 整体校验。
- Buff 伪造等级、魔力不足、冷却、覆盖、取消和过期。
- 无邀请/过期/换连接邀请不能加入；六人上限、队长权限和断线解散。

后端完整验证命令：`mvn -B clean verify`（在 `twinkle-server/` 执行）。测试不需要访问生产服务器，也不依赖参考项目源码或真实客户端。

真实客户端待验收：两名角色登录同频道后，对普通门、背包拖动/丢弃/拾取、药水、商店买卖、加点、基础任务、三个自身 Buff、组队流程逐项录包验证；再补频道切换、满背包、死亡和断线场景。当前未把这一环节标为完成。
